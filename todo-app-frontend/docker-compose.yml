services:
  frontend:
    build: .
    container_name: frontend
    ports:
      - 5173:5173
    environment:
      VITE_BACKEND_BASE_URL: http://localhost:8080
    networks:
      - frontend-net
networks:
  frontend-net:
